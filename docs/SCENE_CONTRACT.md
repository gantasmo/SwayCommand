# Scene contract

A scene is one self-contained ES module in `src/renderer/engine/scenes/`. The engine ([`engine.js`](../src/renderer/engine/engine.js)) renders the active scene (and, during a crossfade, the incoming scene) into offscreen render targets and composites them with equal-power weights, after the Akvj VfxController pattern. Scenes never touch the canvas, the compositor, or each other. Scene instances are created once per engine lifetime and cached for glitch-free switching, so a scene remains valid across repeated activations.

The requirement keywords MUST, MUST NOT, and SHOULD are used as in RFC 2119: MUST is absolute; SHOULD is the default expectation, deviating only where the scene design gives a documented reason.

## Engine integration

The engine renders each active scene into its own `WebGLRenderTarget`, cleared to opaque black, sized to the canvas times the device pixel ratio (capped at 1.75). A fullscreen composite pass then mixes the two targets with equal-power weights (`cos²`/`sin²` of the fade progress) and applies, in order: a beat flash of up to +25 percent driven by `io.beat`, an S-curve soft limiter (`col / (1 + 0.35·col)`), and a vignette. Tone shaping is therefore owned by the compositor; a scene supplies raw emissive color and does not apply its own limiting or vignetting.

Lifecycle: `createScene` runs lazily on a scene's first activation, and the instance is cached in the engine's instance map for the rest of the engine's lifetime. `update` is called only while the scene is visible, as the active scene, or as the incoming scene during a fade. `resize` is called on every cached instance, visible or not. The engine does not call `dispose` mid-performance; the method covers teardown paths.

## Module shape

A scene module MUST export `meta` and `createScene`:

```js
// src/renderer/engine/scenes/<id>.js
export const meta = { id: '<id>', name: '<Display Name>', mood: '<one word>' };

export function createScene(ctx) {
  const { THREE, quality } = ctx; // use ctx.THREE, never import 'three' directly
  // every custom shader is GLSL3: new THREE.ShaderMaterial({ glslVersion: THREE.GLSL3, ... })
  // with in/out and `out vec4 fragColor`, never GLSL1 forms (attribute/varying/gl_FragColor)
  const scene = new THREE.Scene();
  const camera = new THREE.PerspectiveCamera(60, ctx.width / ctx.height, 0.1, 400);
  // build everything procedurally, no textures or assets from disk, no fetch
  return {
    scene, camera,
    update(dt, t, io) { /* animate; allocation-free per frame */ },
    resize(w, h) { camera.aspect = w / h; camera.updateProjectionMatrix(); },
    dispose() { /* dispose every geometry, material, and render target created here */ },
  };
}
```

`meta.id` keys the scene registry ([`scenes/index.js`](../src/renderer/engine/scenes/index.js)) and is the id that project files reference ([PROJECTS.md](PROJECTS.md)). Registration is by import: `scenes/index.js` imports each module and builds `sceneList` (the `meta` objects) and `creators` (id -> `createScene`).

`createScene(ctx)` runs once, on the scene's first activation. The returned object MUST provide:

| Member | Called | Semantics |
|---|---|---|
| `scene` | every frame | The `THREE.Scene` the engine renders. |
| `camera` | every frame | The camera the engine renders with. |
| `update(dt, t, io)` | every frame while active | `dt` is the frame delta in seconds, clamped to 0.05 by the engine; `t` is elapsed time in seconds; `io` is the input snapshot below. |
| `resize(w, h)` | on canvas resize, all cached instances | New canvas size in CSS pixels. |
| `dispose()` | on teardown | Releases every GPU resource the scene created. |

## Creation context (`ctx`)

| Field | Contents |
|---|---|
| `THREE` | The three.js namespace. Scenes MUST NOT import `three` themselves. |
| `renderer` | The shared `WebGLRenderer`. For capability checks only; scenes MUST NOT call `render` or alter renderer state. |
| `width`, `height` | Canvas size in CSS pixels at creation time. Later changes arrive through `resize(w, h)`. |
| `quality` | `{ tier, particles }`, the quality tier (table below). Scenes SHOULD derive instance and particle counts from `quality.particles`. |
| `environment` | A PMREM-filtered room environment texture, generated once by the engine. Reflective scenes set it as `material.envMap` (the theDAW-port chrome scenes do); scenes MUST NOT dispose it. |

Quality tiers:

| `tier` | `particles` |
|---|---|
| `low` | 8,000 |
| `med` | 30,000 |
| `high` | 80,000 |

The engine defaults to `med`; an unknown quality name also falls back to `med`. `quality.particles` is a budget, not a mandate, a scene divides it among its systems (the reference implementation sizes its dust field as `max(500, particles / 12)`). A scene with no per-instance geometry keys off `quality.tier` instead: the fullscreen-quad scenes resolve their shader loop bounds and optional layers into `#define` values so the compiler sees literal, unrollable counts, and `cymatic` picks its icosahedron subdivision detail (12 / 18 / 26) the same way.

## Per-frame input (`io`)

`io` is one shared object, mutated in place by the engine each frame and passed to both scenes during a fade. Scenes MUST treat it as read-only.

| Field | Range | Meaning |
|---|---|---|
| `io.level` | 0 to 1 | Overall audio loudness, smoothed. |
| `io.bands.bass` / `.mid` / `.high` | 0 to 1 | Smoothed band energies. |
| `io.beat` | 0 to 1 | Beat impulse: 1 on beat, exponential decay. |
| `io.xy.x`, `io.xy.y` | 0 to 1 | Hand position, engine-smoothed. The mouse fallback maps the canvas bottom edge to `y = 0`; on Sway hardware the raw CC value passes through unchanged, with `y = 0` as the low/near hand (unconfirmed). |
| `io.gestures.pulse` / `.press` / `.sway` | 0 to 1 | Sway gesture dimensions. |
| `io.knobs[0..7]` | 0 to 1 | Raw knob positions; default 0.5. The values mirror the hardware regardless of what the knobs are assigned to, the factory table (below) routes them through the assignment layer, so scenes SHOULD prefer the gesture dimensions over raw knob reads. |
| `io.pads[0..15]` | 0 to 1 | Pad velocities, indexed as the deck numbers the pads (0 to 15: top rows first, left cluster then right, then the bottom rows). The engine decays each value exponentially after the hit (time constant 0.2 s). |
| `io.lastPad` | −1 to 15 | Most recently struck pad; −1 before any hit. |
| `io.strike` | 0 to 1 | The strike dimension: the maximum pad energy this frame. Scenes SHOULD treat a rising edge as a discrete morph event (a geometry advance, a mode jump, a re-seed) of their own generative parameters. |
| `io.palette` | `THREE.Color[5]` | The current ColorMaster palette. Scenes MUST copy values out and MUST NOT mutate the array or the colors. |
| `io.intensity` | 0.25 to 1.35 | Master brightness: `0.25 + 0.75 × the engine intensity parameter + 0.35 × pulse`. Scenes SHOULD scale emissive output by it. |
| `io.transport` | `{ playing, time }` | The show clock, mirrored from the transport by the router each frame: `playing` is true while the timeline is running, `time` is the position in seconds. A scene that opens on a trigger watches the rising edge of `playing`; nothing else in the contract depends on it. |

Knobs carry no factory assignment: `io.knobs` mirrors the hardware, and nothing is driven until the project assigns it ([STUDIO.md](STUDIO.md)); the factory templates map hue (`engine:hue`, center detent), fade length (`engine:fadeTime`, 1 to 8 s), and kit level (`sampler:master`) on knobs 1, 2, and 8. A scene that reads a raw knob says so in its inventory entry (Beam Sixteen's beam-style morph on `io.knobs[3]`, KNOB 4; Nature's Tomb's development on `io.knobs[4]`, KNOB 5, organism on `io.knobs[5]`, KNOB 6, and species on `io.knobs[6]`, KNOB 7; Miracle Mile's yield on `io.knobs[4]`, KNOB 5, and act on `io.knobs[5]`, KNOB 6). Those raw reads are the no-assignment fallback; the same scenes declare the controls properly through the scene control surface below, so a performer can put them on any control.

## Bloom

The engine runs an UnrealBloom pass per scene over the half-float composite. A scene requests it with `meta.bloom = { strength, radius, threshold }`, or by exposing a live `bloom` object on the returned instance that `update()` mutates (the live object wins). Strength crossfades with the scene mix; absent both, the pass is skipped. The theDAW ports use upstream's numbers (the lattice 0.2 × per-shape multipliers, the chrome scenes 1.65 / 0.4 / 0.6).

## The scene control surface

A scene MAY declare its own controls, which puts its events and parameters in the assignment panel next to the engine, rack, synth and kit targets, so a black hole, an organism, or a yield is bound to a pad, a knob, a button or a gesture the same way everything else is, instead of riding a raw knob by convention.

The declaration is static, in `meta`, so the panel can list a scene's controls without instancing it:

```js
export const meta = {
  id: 'willidream', name: 'Will I Dream', mood: 'lucid',
  controls: {
    actions: [{ key: 'blackhole', label: 'black hole' }],
    params: [{ key: 'objectScale', label: 'object scale', min: 0.2, max: 6, default: 1 }],
  },
};
```

The instance implements one method per kind:

| Member | Called | Semantics |
|---|---|---|
| `action(key)` | on a pad strike, a button press, or a timeline-driven event bound to `scene:<id>:<key>` | Fires a discrete event. The engine dispatches it **only while the scene is on screen** (the active scene, or the incoming scene mid-fade), because an event fired at a scene nobody can see has nowhere to land. |
| `setParam(key, value)` | on every knob or gesture change bound to `scene:<id>:<key>` | Sets a continuous parameter. `value` arrives already mapped through the assignment's curve and min-max range. Parameters reach a cached instance whether it is on screen or not, they are state, and a scene should come up holding the value the performer left. |

Targets carry the scene id (`scene:<sceneId>:<key>`) because two scenes may name a parameter the same and a control reaching for one must never move the other. A pad binds an event through its own action type, `{ type: 'sceneAction', scene, action }`; knobs, gestures and buttons use the target string. Labels are terse lower-case phrases; the panel groups them under the scene's name.

Declaring controls does not remove a scene's raw `io.knobs` reads. Those stay as the no-assignment fallback, so a scene still performs out of the box on a deck nobody has configured, and the inventory below records which knobs a scene reads that way.

## Morph responses

Strike and sway are morph dimensions, not motion inputs: sway SHOULD continuously reshape a core generative parameter of the scene (mode numbers, field strengths, warp exponents, fold counts), and a strike SHOULD fire a discrete restructuring (a geometry advance, a mode jump, a basin or seed jump). A response that only rotates, leans, or orbits an object does not satisfy this.

## Hard rules

1. **Palette-driven color.** All visible color MUST derive from `io.palette`; lightness and saturation shifts are permitted. Uniforms holding colors MUST be refreshed with `.copy()` every frame, the palette animates (crossfades on project load, live hue rotation from knob 0).
2. **Audio-reactive and gesture-reactive.** At minimum, one element MUST breathe with `io.bands.*` or `io.beat`, and one element MUST follow `io.xy` continuously. Pads SHOULD cause visible discrete events where they fit the scene.
3. **No per-frame allocation.** Vectors, arrays, and buffers MUST be preallocated in `createScene`. `update` MUST NOT construct objects; scratch objects created once outside `update` are permitted.
4. **Self-contained.** A scene MUST NOT import anything beyond what `ctx` provides, and MUST NOT load assets, fetch, access the DOM, or read window globals. The one permitted import is a scene-private helper module in a sub-directory named after the scene (`scenes/naturestomb/weather.js`, `scenes/miraclemile/launchmap.js`): a factory taking `THREE` and what else it needs as arguments, exporting no `meta`, never registered, owned and disposed by the scene that imports it, a way to keep a big scene's independent subsystems in separate files and separate shader programs, not a way to share code between scenes. Shaders are inlined as template strings and MUST be GLSL3 (`glslVersion: THREE.GLSL3`, `in`/`out`, `out vec4 fragColor`, `texture()`); GLSL1 forms (`attribute`, `varying`, `gl_FragColor`, `texture2D`) are not accepted. Built-in materials patched through `onBeforeCompile` follow three.js's own shader conventions by necessity.
5. **Dark-background friendly.** The engine clears each render target to black and the compositor blends additively toward black. Scenes MUST read well on a black background and SHOULD avoid full-screen solid fills.
6. **Performance.** A scene MUST hold 60 fps at 1080 p at `quality.tier === 'med'` on a mid-range integrated GPU. Scenes SHOULD render each system in one draw call (instancing, or `BufferGeometry` with a custom `ShaderMaterial`) and MUST NOT spawn per-object `Mesh` instances in large numbers.
7. **Complete disposal.** `dispose()` MUST release every geometry, material, and render target the scene created. `InstancedMesh` requires its own `.dispose()` call, which frees the `instanceMatrix` and `instanceColor` GPU buffers.
8. **No autonomous rotation.** Nothing in a scene rotates, orbits, spins, rolls, or drifts by itself, not the camera, not a whole object, not a pattern (a kaleidoscope fold, a ring, a field of spikes). Rotation driven directly by the hand, a gesture, or a strike is allowed; scrolling, flowing, rippling, breathing, pulsing, per-particle flow, and path travel are not rotation. The same rule binds the effects rack.

## Reference implementation

[`scenes/beams.js`](../src/renderer/engine/scenes/beams.js) is the reference implementation; new scenes match its style, structure, and comment density. It demonstrates a single `InstancedMesh` for the sixteen columns, per-frame `.copy()` of palette colors into a preallocated `THREE.Color`, preallocated matrix/vector scratch, pad energy folded into per-column flash state, and a `dispose()` that covers the instanced mesh, its geometry and material, the particle system, and the grid helper.

## New scenes

1. Create `src/renderer/engine/scenes/<id>.js` exporting `meta` and `createScene` per the module shape above.
2. Import the module in [`scenes/index.js`](../src/renderer/engine/scenes/index.js) and append it to the `modules` array; `sceneList` and `creators` derive from that array.
3. Add the id to the Auto-VJ pool of at least one project or template ([PROJECTS.md](PROJECTS.md)) so the scene enters a rotation; the SCENES bank lists every registered scene regardless.
4. Run `npm start` to rebuild the renderer bundle and launch.

## Scene inventory

The registry holds sixteen scenes, listed here in registry order. Registry order fixes nothing on the keyboard, the digit keys index the active project's pool ([ENGINE.md](ENGINE.md#scene-management)).

| Id | Name | Mood | Mechanism |
|---|---|---|---|
| `beams` | Beam Sixteen | anthemic | Sixteen vertical light beams, one per Sway IR sensor, as one `InstancedMesh` of axial-billboard quads whose fragment shader blends three beam styles on a continuous style parameter read from the raw knob `io.knobs[3]` (KNOB 4; the knob itself is the transition (a flick crossfades in ~0.3 s, a slow turn dissolves over the whole sweep): HOLOGRAM (translucent cylinder body, fresnel rim with a chromatic split, scanlines scrolling upward, interference bands, flicker), LASER (a ~2.6 px white-hot gaussian core, a wide palette halo that bass and pads fatten, an impact bloom at the floor, drifting haze) the rest look), ELECTRICITY (the core path displaced by noise that translates along the height, bright travelling nodes, hard gating, an intermittent secondary branch). A floor plane carries per-beam impact pools and reflection streaks; dust motes brighten as they drift through the beams. The hand sweeps a highlight across the row and lifts the eye, sway morphs the field from a parallel array into a fanned, cross-tilted lattice, a strike re-seeds the formation, pads flash their own column. Fixed eye, nothing rotates. Three draw calls. |
| `swarm` | Swarm | hypnotic | Stateless GPU particle cloud around an attractor that chases the hand; positions are computed in the vertex shader from random seeds and time, so the CPU pushes only uniforms. Bass swells the orbits, beats and pads detonate a radial burst, press clenches the swarm; sway morphs the flocking from one murmuration into five sub-flock cells that breathe on a fixed ring (the cells do not revolve), and a strike is a scatter shock, a velocity burst plus an attractor re-seed the swarm reforms from over ~2 s. Fixed eye with a beat lift. One `Points` draw call. |
| `ribbons` | Ribbons | fluid | Glowing triangle-strip trails on per-ribbon Lissajous paths, blended toward the hand; mid band and press fatten the strips, pads teleport a ribbon head with a flash. Sway morphs the paths from long silk sweeps to tight coils (rate up, amplitude and width down); a strike is a whipcrack, one amplitude impulse runs head to tail down every strip over 0.9 s. One draw call for all strips plus one for head sprites. |
| `voxels` | Voxels | chunky | An N × N slab of boxes forming a living heightfield; bass pumps radial waves from the center, pads drop expanding ripples, the hand drags a gaussian hill, press crushes the field flat. |
| `nebula` | Nebula | psychedelic | Three value-noise fbm layers domain-warp each other into gas folds on one quad, mirror-folded into a fixed-axis kaleidoscope whose fold breathes open and shut (the mirrors never spin), contoured into neon filaments and coloured by a five-stop palette ramp; the hand pans the domain with parallax, mid drives warp strength, bass stacks concentric shells, treble scintillates, the beat lurches the domain, and each pad fires an expanding shockwave from its own cell of a 4 × 4 screen grid. Sway is the turbulence morph, it re-weights the fbm octave gain, rotates the flow from gradient push to curl, and folds ridged cell walls into the density, laminar drift to storm cells; a strike is ignition, a burst core at the centre shoving a density wave outward through the field. One draw call. |
| `mandelbulb` | Mandelbulb | infinite | Raymarched fractal solid on one fullscreen quad with six distance estimators (Mandelbulb, Mandelbox, Julia bulb, Menger sponge, Sierpinski tetrahedron, Apollonian) picked by pad index modulo six; every strike also hops the iteration-constant seed by the golden angle, so a hit on the same formula still lands in a new basin, under a detonation flash. Sway glides the primary parameter (the bulb's power, 6 to 12), press squeezes the secondary fold while diving the camera, bass breathes the primary parameter around the hand's setting; every gesture runs through long exponential smoothing so the formula drifts rather than snaps. Rays that miss paint neon haze striped by the active formula's symmetry order. One draw call. |
| `cymatic` | Cymatic Orb | resonant | A tessellated icosahedron displaced in the vertex shader by three band-driven spherical standing-wave modes plus a zonal ring term; the fragment shader re-evaluates the same field and lights its zero set as nodal Chladni lines. Sway re-tunes all four mode numbers at once, sparse low-order figure to dense high-order weave; a strike snaps the mode stack to a fresh figure with a ~1.2 s ring-down; beats and pads launch travelling ripples; press collapses the displacement while the lines burn white. Two draw calls, the second a back-facing aura shell. |
| `spectra` | Spectra | spectral | A 256 × 256 scrolling mel-spectrogram history in a `DataTexture` displaces a 50 × 50 plane upward, the terrain is the spectrogram, frequency mirrored about the center, time running away from the camera, flanked by curved spectrum walls and an additive particle field. Pads 0 to 6 pick the color theme and 8 to 12 the camera mode, the hand steers the live camera, press compresses the relief; sway morphs the layout itself (log <-> lin axis warp, the mirror re-folding into a multi-fold comb, a radial burst), and a strike is a spectral slam, a full-band impulse enters the synthesized source spectrum and rides upstream's mel / EMA machinery through terrain and walls while the layout seed steps. Ported from GANTASMO VJ-9000's SpectraRenderer. |
| `vjshader` | VJ Shader | kaleidoscopic | Five raymarched fragment-shader presets, Mandelbulb, Julia bulb, Mandelbox, kaleido IFS on pads 0 to 3 and a Menger flythrough on pad 4, with the upstream eight-material picker on pads 8 to 15, on one fullscreen quad; pads 5 to 7 step to the next preset. The hand sets the camera orbit angles (no self-drift), an audio envelope scrubs colour and the flythrough position, press biases the active preset's primary parameter; sway is a preset-space morph, every structural parameter carries a per-preset morph vector, zero at rest, so the hand glides the solid through distinct pattern families, and a strike kicks the primary parameter, seed-jumps the wheel scrub, and flashes. Ported from GANTASMO VJ-9000's shader presets; the Menger preset retains its upstream MIT attribution. |
| `ferrofluid` | Ferrofluid Orb | magnetic | theDAW's cymatics orb: a black-chrome icosphere whose vertex shader (`sphere-shader.ts`, verbatim) grows Rosensweig spikes in a Fibonacci phyllotaxis at the field poles, reflecting the engine's shared PMREM room environment under a three-point rig, with the per-scene UnrealBloom at upstream's 1.65 / 0.4 / 0.6. A strike re-magnetizes (spike density steps 3 -> 5 -> 7 -> 9 with an amplitude surge), sway glides density 2 to 9 and viscosity from oily swells to a needle forest, press crushes the spikes toward mirror chrome as reflectivity rises, pulse surges bloom and amplitude, the hand orbits the camera. Apache-2.0 port, notice retained. |
| `chladni` | Cymatic Plate | resonant | theDAW's Chladni/Faraday standing-wave liquid plate (`cymatics-shader.ts`, verbatim): sixteen n/m mode tables excited by a spectrum synthesized from the three bands, with a spectral-centroid mode picker and quadrature traveling waves, cover-framed so the damped rim never shows. A strike jumps the mode index by 3 to 5 with a decaying amplitude hit, sway bows the mode target across the whole table, press damps the plate (amplitude down, time × 0.4), pulse drives it (amplitude 1 -> 2 plus a bloom surge), the hand drifts the eye over the surface. Apache-2.0 port, notice retained. |
| `valley` | Chrome Valley | synthwave | theDAW's infinite-scroll chrome valley (`landscape-shader.ts`, verbatim): a mountain-walled terrain of stacked sine octaves under a fbm plasma sun with a fresnel halo, a palette point light, and a 240-star sky, reflecting the shared room environment. Sway crossfades the whole terrain from chrome landscape to golden-angle quasicrystal ferrofluid (`isFerrofluid` 0 to 1) and biases the scroll speed; a strike is a seismic hit (mountain height +1.2 decaying ~1.5 s, plasma and bloom flaring); press dives the camera between the ridges as the fog pulls in; pulse surges sun and stars; the hand drifts the camera across the valley. Apache-2.0 port, notice retained. |
| `lattice` | Quantum Lattice | crystalline | theDAW's lattice engine at full fidelity: 373 instanced nodes and 756 beams with energy filaments and a reactor core, morphing between four geometries (Grand Torus, Cubic Frame, Merkabah Star, Cosmos Cage) with positions pouring toward their targets at 0.06/frame and a morph-energy shockwave, under live bloom (0.2 × per-shape multiplier, plus volume and shockwave). A strike advances the geometry and fires the full shockwave; sway morphs the filament field (wave detail plus a continuous morph push, so the lattice writhes and unravels); pulse surges bloom and the reactor heartbeat; press dives the camera; the hand steers the orbit. MIT-licensed upstream. |
| `willidream` | Will I Dream | lucid | The universe begins on the downbeat and you fly out of it. COLD OPEN: the scene opens dark, the whole particle population collapsed onto one faint singularity breathing with the level, and waits for the show. BIG BANG: on the rising edge of `io.transport.playing` (or the `big bang` action, or any pad while dark) the singularity ignites, Majid Manzarpour's "Big bang simulation three.js" (MIT; ported: positions rebuilt in the vertex shader from velocity and one expansion scalar, constant expansion replaced by a settling one, the sprite replaced by the star field's own analytic capsule, the galaxy stage folded into the same population, the nebula stage played by the sky quad, palette-driven, no orbit), 20,000-odd particles leave the point along their own velocities, cool from white-hot to the palette, gather into clusters, and then WITHOUT a crossfade hand over one by one into their slots in the star field and stream on: the bang is the field's first ten seconds. STOP then PLAY restarts the universe, PAUSE then PLAY resumes the flight; the restart is read off the transport position (a clock at the top or moved backwards since the scene last looked, a stop, a scrub, a loop seam), so a show restarted while the scene was off screen still bangs, and a scene arriving mid-show opens in the flight. FLIGHT and HYPERSPACE as before: the stars are the streaks, X speed and closeness density, sway scatter <-> lattice, past 0.55 the jump engages and under 0.25 it ends with a flash and a new object ahead (the `hyperspace jump` action runs the envelope from a pad). OBJECTS: thirteen bodies on one camera-facing quad, pulsar, spiral galaxy, solar system, the Nebula Madness remnant, ringed giant, Halo ringworld, a part-built Dyson shell with the star burning through its unstarted sectors, a derelict shipyard whose debris tumble is sway's, a volcanic world whose fissures are the only light across the terminator, an ice world, a banded gas giant with fixed storm ovals, an ocean world with a sun glint, a terrestrial world with city lights on its night side, each GROWING from a point at its arrival over 1.6 s, never popping in at size. `object scale` applies live; `object x`, `object y`, `object distance`, `next object` apply to the NEXT arrival (each axis keeps the scene's scatter until moved), and the size has a ceiling at the body's distance mapped onto the knob's travel so the top of the range always reaches it. WORMHOLE: its own event, a throat showing another sky opens ahead, the field bends in on a thin-lens deflection, the mouth swallows the frame, the tunnel wall is the sky you left and the aperture ahead the sky you are going to, and you are out in a re-seeded field with a new object. BLACK HOLE on PAD 7 as before (Huffman's lens over the procedural panoramic sky, the swallow, the void, re-emergence with no object); PAD 8 / PAD 15 bank; any other pad thrusts. Every event and parameter is on the control surface: actions `bigbang`, `blackhole`, `hyperspace`, `wormhole`, `bankLeft`, `bankRight`, `thrust`, `spawn`, `swallow`; params `objectScale`, `objectX`, `objectY`, `objectDistance`, `objectNext`, `warpAmount`, `starDistribution` (the last two take the max of the hand and the assignment). Four draw calls (sky, stars, object, overlay); bloom live through the bang only. |
| `naturestomb` | Nature's Tomb | cellular | Seven organisms under dark-field light on one plate, in the order of life and then its end. COLD OPEN: the plate is dark until the show starts (the first beat, the transport, or any pad); then the molecule comes up and begins to replicate, the helix stepping to its fork, and the next organism on the knob is the cell. KNOB 6 (`io.knobs[5]`) picks the organism in seven bands, DOUBLE HELIX, CELL LINE, MYCELIUM, SLIME MOLD, TOXIN, PHAGOCYTOSIS, DECOMPOSITION, dissolving as fast as the knob turns; KNOB 5 (`io.knobs[4]`) sets the development 0 to 1 and any strike steps it one stage of the organism on screen, reversing at the ends; KNOB 7 (`io.knobs[6]`) picks one of eight seeded species. THE CELL LINE: sixteen stages on one continuous axis, every boundary a morph, cleavage 1 -> 2 -> 4 -> 8 -> 16 -> 32 -> 64 blastomeres at conserved cell volume (seats relaxed once at creation into a jammed packing, a smooth-min furrow, each pair drawn with the wall it shares, the heap clamped inside a fixed zona so the outer cells flatten against it), a BLASTULA (the cells migrate onto a closed 96-cell shell around a lit blastocoel), the PROTISTS, amoeba, paramecium, euglena, diatom, radiolarian, and PRIMITIVE MULTICELLULAR forms, Volvox, a filament, a sponge. Aggregate <-> protist crossings pour the cells onto the organism's own surface before the analytic body comes up under them; protist <-> protist crossings mix the two fields. Up to 96 cells are culled per ray (eight octant bounding spheres, then a fourteen-cell local set) before the march, so the cost is bounded; nuclei are hit analytically on the cell the ray entered. SLIME MOLD: the Physarum agent simulation with every parameter species-dependent, sensor geometry, turn, deposit, decay, density, a random-turn rate, one to four inoculation points each with its own reach so the colony covers the dish at full development, food layouts, over four archetypes the seed picks between (fine reticulum, coarse trunk veins, broad fan front, a network anchored to its food); a flake lights the moment an agent steps on it. MYCELIUM: per-species germ tubes, branching angle and interval, internode, tortuosity, radial tropism, gravitropism, negative autotropism, anastomosis closing loops, rhizomorph cords, sporangia on late tips; re-run from one random table as sway moves. DOUBLE HELIX: B-form DNA, antiparallel backbones (the phosphate beads show the sense), 10.5 bp per turn, the second strand 140° round the axis for the groove ratio, A-T two and G-C three bonds tinted from the palette; the level runs duplex -> replication fork (leading strand, Okazaki fragments, polymerase bubble) -> nucleosome-wrapped chromatin; sway sweeps torsion from an open ladder through B-form into a writhing supercoil; nothing spins, X is the molecule's azimuth. TOXIN: the same duplex under real molecular models of 2,3,7,8-TCDD (four of them, the fused-ring dioxin, two oxygens bridging the middle ring, chlorinated at 2,3,7,8) and DDT (three, a tetrahedral carbon carrying a trichloromethyl group and two para-chlorophenyl rings), built from their own atom positions at real bond lengths and drawn at the molecule's own scale against the helix, as lit ball impostors on stick bonds coloured by element; they drift in from the dark on a slow wander and tumble only under the hand and the sway. The development is the attack: adducts dock on the bases and the backbone, the two dioxins INTERCALATE, wedging their step of the stack open by a full rise and unwinding the duplex 26° locally, eased over the neighbouring pairs so the backbones bend rather than kink, three NICKS cut one backbone each (a cut segment's ends draw into its middle, so the strand frays and parts), then two DOUBLE-STRAND BREAKS at the intercalation sites and the three fragments carry apart, each displaced and tilted about its own fixed axis with a slow wander once loose, whatever molecule docked on a fragment riding it. A per-base damage level, carried in the segment's own attribute, sickens the backbone and bases toward the palette's cold end and dims them, worst where the molecules struck. PHAGOCYTOSIS: a macrophage consuming a smaller cell, one smooth union on the quad, an amoeboid body under slow streaming bulges, up to seven pseudopods (each a chain of three tapered capsules, bounded by its own sphere and blended into the body only where the blend can reach), and the phagosome. The level runs the sequence: the prey drifts in, the pseudopods reach and WRAP round it along an arc, the membrane SEALS behind it into a phagosome grown from nothing, the vacuole is drawn INSIDE, the prey is compressed and DIGESTED as the vacuole shrinks; sway grows the last three pseudopods and winds tortuosity into all of them, press squeezes the cell, the nucleus is hit analytically through the cytoplasm. DECOMPOSITION: a dead tissue mass of fifty-four cells in two mounded layers, marched by the same culled cell machinery as the embryo, the turgor goes and it SLUMPS (the neck swells and the junctions blur into one mass), the colour DRAINS to the palette's ash end (the least saturated stop relative to its own brightness, so a dark violet is never taken for ash), the surface PITS on a noise the sway moves through, BACTERIA colonise it in patches, the MOULD germinates on the body itself, the hyphal simulation run from its own strain, germ tubes starting at the mass's cells and every segment lifted onto the top of the tissue beneath it, so the network follows the body down, then it BREAKS into five pieces as the neck lets go again and they SINK and dissolve into the substrate, leaving the network and a mottled ashen STAIN. Sway morphs each organism, press squeezes, the hand pans and dollies. Control surface: actions `cellLine`, `slimeMold`, `mycelium`, `doubleHelix`, `toxin`, `phagocytosis`, `decomposition`, `developUp`, `developDown`, `reseed`; params `development`, `organism` (0 to 6), `species`, `morph`, `squeeze` (morph / squeeze take the max of gesture and assignment). Three draw calls (the quad, the hyphae mesh, the fungus or the mould on the corpse, the helix mesh, the molecule and the toxin's molecules with it). |
| `miraclemile` | Miracle Mile | critical | The atom, and the city it ends. COLD OPEN: the scene opens dark on the collider, a faint detector breathing with the level, until the show starts (the first beat, the transport playing, or any pad), and the first collision fires as the opening element. Four acts on the raw knob `io.knobs[5]` (KNOB 6, quarter turns: COLLIDER, FISSION, DETONATION, the 0.5 rest, SHOCKWAVE; the `act` param drives the same choice), the raw knob `io.knobs[4]` (KNOB 5) or `yield` as the yield. COLLIDER and FISSION as before (the detector barrel and a strike's helix-track collision in the live solenoid field; thirteen nuclei and a strike's neutron starting the chain reaction; sway the field / the scission symmetry, press the dive / the compression, the hand the orbit). THE CUT, FISSION -> DETONATION is a continuous scale match, not a dissolve: the lattice hangs 46 m over the boulevard and the act change is one straight pull-back on a log schedule (11 -> 520 m) during which every nucleon travels to a REAL lit window (the JS runs the shader's window rule verbatim over the same lot hash, the same masses and the same damage field, matches by where each sits in frame, and the point arrives holding the pane's own colour); reversing the knob runs it back, `transition` scrubs it; the MIRV deck stands down for the move. THE CITY, a sunset megalopolis under both city acts: a DDA over 30 m lots (avenues, cross streets, the boulevard, a river bridged by it, a rail yard) of six architectural families per lot, deco setback tower (the few tall ones), brick pair (two walk-ups side by side, water tank, fire escape), glass slab (curtain wall reflecting the dome, plant room, mast), shed (corrugated, ghost sign, an LED billboard on the roof), concrete mid-rise (balconies, plant box), podium + slab, a sea of 4 to 25 storeys with a few towers, every lot its own footprint, heights, window pitch, lit fraction, colour temperature, palette tint, signage; materials with micro-variation, grime and a wet sheen, brick courses and panel joints at the pavement's range; every lit pane a ROOM the view ray is carried into (lit ceiling, furniture against the back wall, blinds, a figure at the glass in some) so interiors slide against the facade as the eye moves; panes reflecting the dome and the sun as one hard glint, rain running down the glass; neon tubes and LED boards of palette colour (dot-matrix glyph columns, a third scrolling); the megalopolis continuing past the DDA's reach as two hazed skyline bands with scattered lights; aerial perspective, fog banks, rain, steam, lamp streaks in wet asphalt that carries the dome and the sun. THE SKY, a pretty sunset: the sun low on the left, a warm gradient, cirrus lit from beneath, a cumulus deck, a storm cell on the right throwing a forked bolt every so often (`lightning` fires one), stars as the dusk deepens; one hard low sun whose shadow is two height lookups along its bearing, the dome as ambient, occlusion at the canyon floor. Every burst adds to a WRECK level decaying over minutes: the ash overcast creeps in, the sun reddens and dims, the haze greys and thickens, the lamps come up. THE WRECK, the fronts leave damage that persists until `rebuild`: every burst a crater in a damage field (one byte a lot on a texture the shader samples and the JS mirror reads; a crater falloff plus the front's wake out to 4.5 radii): blocks knocked to stumps inside the heavy radius (exposed slabs, rubble), panes blown out past their own threshold (dark holes; one in five burning), fire sites catching as the front reaches them and standing leaning smoke columns (dust as the front passes, black as the fire takes, lit from the foot, burning out over minutes), signage dimming, lamp posts knocked over, and a debris shower of dark chunks riding every front past the eye. DETONATION from ~86 m up (above most of the skyline, so the bursts tower over it), SHOCKWAVE from the pavement; ground zero fires on `detonate` or a strike with press ≥ 0.8 (the unassigned-deck fallback). MIRV, in the two city acts every pad is an independent re-entry vehicle on a deterministic map of two RINGS round the eye (top row the far ring 600 to 950 m across ±48°, bottom row the near ring 180 to 380 m across ±70°, the eye sees ±40°, so the near bursts wrap past the frame's edges and tower over it), yield falling with pitch on `0.30 + 0.70·(1 − i/15)^1.35` (pad 0 a city-killer standing a minute, pad 15 a tactical burst); the burst lands ON the hit: a 0.14 to 0.24 s STREAK down a shallow twenty-degree line from 1.4 km out, a hard white head and a thin bright streak of ionised air behind it, sized in angle, no smoke, gone as an afterimage in under half a second, occluded by the city and the standing clouds through a CPU visibility query along the track; then flash, fireball, cap, stem, condensation cap, and a front whose shell and whose blackout of the grid are one radius law. Five cloud slots on med (eight high, four low), two marched per ray and the rest composited coarsely rather than dropped, packed uniforms with a uniform loop bound, per-cloud constants hoisted to the CPU, the cap's roll fading out on the axis where its mapping flips, a 3.5 s density fade at the end of a cloud's life, eviction by the slot furthest through its life; `salvo` fires exactly a deck's worth. Control surface: actions `collide`, `split`, `detonate`, `blast`, `strike`, `salvo`, `rebuild`, `lightning`; params `act`, `yield`, `transition`, `cloudScale`, `place`. Four draw calls (world quad, capsules, solid impostors, nucleons and debris, additive impostors) and one 128² damage texture rebuilt on each burst; live bloom rides the flash. 1080p tier med: the sunset city at rest 14.3 ms (the old city 11.4 in the same run), the street 13.9 (10.4), ground zero six seconds in 15.1 (10.4), four near-ring bursts three seconds after a salvo 21.9 (16.0), the deck state over the contract as before and ~6 ms dearer than it was (death by a thousand cuts: the sky, the rooms, the sun shadow, the wake loops, the smoke; each measured under 1.5 ms alone); the wrecked city with standing bursts and smoke columns 27. Absolute numbers move ±15 % with the GPU's clocks; compare within a run. |
